#!/usr/bin/env bash
# Out of 3.7 scope — Streams-group admin APIs are post-3.7 work
# tied to the next-gen consumer rebalance protocol (KIP-848 / KIP-1071).

. "$(dirname "$0")/_common.sh"
skip "Streams-group admin (post-3.7 / KIP-848 ecosystem); out of scope"
