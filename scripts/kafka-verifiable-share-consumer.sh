#!/usr/bin/env bash
# Out of 3.7 scope — verifiable share-group consumer is KIP-932 tooling (Kafka 4.0+).

. "$(dirname "$0")/_common.sh"
skip "KIP-932 verifiable share consumer (Kafka 4.0+); out of 3.7 parity scope"
