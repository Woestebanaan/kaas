---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kafkaclusters.skafka.io
spec:
  group: skafka.io
  names:
    categories: []
    kind: KafkaCluster
    plural: kafkaclusters
    shortNames: []
    singular: kafkacluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .spec.listeners.external.enabled
      name: External
      type: boolean
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for KafkaClusterSpec via `CustomResource`
        properties:
          spec:
            properties:
              listeners:
                default:
                  external:
                    enabled: false
                    gateway:
                      enabled: false
                      gatewayRef: {}
                    port: 9093
                    service: {}
                    tls:
                      certManager:
                        enabled: false
                        issuerRef: {}
                  internal:
                    port: 9092
                properties:
                  external:
                    default:
                      enabled: false
                      gateway:
                        enabled: false
                        gatewayRef: {}
                      port: 9093
                      service: {}
                      tls:
                        certManager:
                          enabled: false
                          issuerRef: {}
                    properties:
                      bootstrapHostname:
                        description: Optional convenience hostname added to the certificate SANs. Not required for operation.
                        type: string
                      enabled:
                        type: boolean
                      gateway:
                        default:
                          enabled: false
                          gatewayRef: {}
                        properties:
                          enabled:
                            type: boolean
                          gatewayRef:
                            default: {}
                            properties:
                              name:
                                type: string
                              namespace:
                                type: string
                            type: object
                        required:
                        - enabled
                        type: object
                      hostnamePattern:
                        description: Go-fmt-style `%d` for the broker ordinal, e.g. `broker-%d.kafka.example.com`.
                        type: string
                      port:
                        default: 9093
                        description: '`default: 9093` in the committed CRD (`...kafkaclusters.yaml:88`).'
                        format: int32
                        type: integer
                      service:
                        default: {}
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            type: object
                        type: object
                      tls:
                        default:
                          certManager:
                            enabled: false
                            issuerRef: {}
                        properties:
                          certManager:
                            default:
                              enabled: false
                              issuerRef: {}
                            properties:
                              enabled:
                                type: boolean
                              issuerRef:
                                default: {}
                                properties:
                                  kind:
                                    description: '`ClusterIssuer` or `Issuer`. Empty defaults at reconcile time (operator side; we don''t apiserver-default it — gh #137).'
                                    pattern: ^(ClusterIssuer|Issuer)?$
                                    type: string
                                  name:
                                    type: string
                                type: object
                            required:
                            - enabled
                            type: object
                        type: object
                    required:
                    - enabled
                    type: object
                  internal:
                    default:
                      port: 9092
                    properties:
                      port:
                        default: 9092
                        description: 'The committed CRD ships `default: 9092` (verifiable at `deploy/crds/skafka.io_kafkaclusters.yaml:127`). Apiserver defaulting fills this when the chart omits it. Schemars renders the helper via `#[schemars(default = ...)]`.'
                        format: int32
                        type: integer
                    type: object
                type: object
              replicas:
                format: int32
                minimum: 1.0
                type: integer
              storage:
                default: {}
                properties:
                  className:
                    type: string
                  size:
                    type: string
                type: object
            required:
            - replicas
            type: object
          status:
            nullable: true
            properties:
              bootstrapServers:
                items:
                  type: string
                type: array
              conditions:
                items:
                  description: One status condition. Field shapes mirror apimachinery's `metav1.Condition` exactly.
                  properties:
                    last_transition_time:
                      description: RFC 3339 timestamp. Required.
                      type: string
                    message:
                      description: Human-readable. Required, up to 32768 chars.
                      maxLength: 32768
                      type: string
                    observed_generation:
                      description: '`metadata.generation` observed at transition. Optional; must be ≥ 0 when set.'
                      format: int64
                      minimum: 0.0
                      nullable: true
                      type: integer
                    reason:
                      description: Programmatic identifier. Required, CamelCase, 1..=1024 chars.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: '`True`, `False`, or `Unknown`. Required.'
                      pattern: ^(True|False|Unknown)$
                      type: string
                    type:
                      description: CamelCase or `foo.example.com/CamelCase`. Required.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - last_transition_time
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        title: KafkaCluster
        type: object
    served: true
    storage: true
    subresources:
      status: {}
