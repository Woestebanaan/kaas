---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kafkaclusterassignments.kaas.rs
spec:
  group: kaas.rs
  names:
    categories: []
    kind: KafkaClusterAssignments
    plural: kafkaclusterassignments
    shortNames: []
    singular: kafkaclusterassignments
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.controller
      name: Controller
      type: string
    - jsonPath: .status.controllerEpoch
      name: Epoch
      type: integer
    - jsonPath: .status.assignmentVersion
      name: Version
      type: integer
    - jsonPath: .status.truncated
      name: Truncated
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for KafkaClusterAssignmentsSpec via `CustomResource`
        properties:
          spec:
            type: object
          status:
            nullable: true
            properties:
              assignmentVersion:
                description: Controller-local monotonic counter, bumped on every write within a single controller's tenure.
                format: int64
                type: integer
              brokers:
                items:
                  properties:
                    health:
                      description: '`alive`, `draining`, or `dead`.'
                      enum:
                      - alive
                      - draining
                      - dead
                      pattern: ^(alive|draining|dead)$
                      type: string
                    id:
                      type: string
                    lastSeen:
                      type: string
                  required:
                  - health
                  - id
                  type: object
                type: array
              consumerGroups:
                items:
                  properties:
                    broker:
                      type: string
                    epoch:
                      format: int64
                      type: integer
                    groupId:
                      type: string
                  required:
                  - broker
                  - epoch
                  - groupId
                  type: object
                type: array
              controller:
                description: Broker ID currently holding the controller Lease.
                type: string
              controllerEpoch:
                description: '`leaseTransitions` of the `kaas-controller` Lease at write time. Brokers reject `assignment.json` files with stale epochs.'
                format: int64
                type: integer
              generatedAt:
                description: RFC 3339 wall-clock time the controller produced this assignment.
                type: string
              partitions:
                items:
                  properties:
                    broker:
                      type: string
                    epoch:
                      format: int64
                      type: integer
                    partition:
                      format: int32
                      type: integer
                    role:
                      description: '`leader` (only role today; reserved for future).'
                      pattern: ^(leader)?$
                      type: string
                    topic:
                      type: string
                  required:
                  - broker
                  - epoch
                  - partition
                  - topic
                  type: object
                type: array
              truncated:
                description: '`true` when the partition list was clipped to fit under the 1MB K8s object size limit. Inspect the file on the PVC for the full list when set.'
                type: boolean
            type: object
        required:
        - spec
        title: KafkaClusterAssignments
        type: object
    served: true
    storage: true
    subresources:
      status: {}
