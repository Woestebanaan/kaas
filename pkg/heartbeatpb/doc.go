// Package heartbeatpb holds the generated gRPC stubs for the cluster
// controller / broker heartbeat protocol defined in proto/heartbeat.proto.
//
// The generated *.pb.go and *_grpc.pb.go files are produced by `make proto`
// (Phase 4 work) and are intentionally absent in Phase 1 — only this doc.go
// file exists today so that go tooling treats the directory as a real
// package.
package heartbeatpb
