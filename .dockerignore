# Rust workspace .dockerignore. Applies to bins/*/Dockerfile builds whose
# context is the repo root. The archive/ tree has its own .dockerignore for
# the legacy Go build.
.git
.github
target/
archive/
deploy/
tests/
*.md
.claude
.idea
.vscode
*.swp
