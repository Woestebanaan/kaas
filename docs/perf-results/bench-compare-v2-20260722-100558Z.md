# bench-compare-v2 — 2026-07-22T10:05:58Z

> Generated by `.claude/skills/bench-compare-v2/run-bench-compare-v2.sh`.
> Single-run measurement — prefer 5-run averages (feedback_bench_methodology).

## Configuration

| Setting | Value |
| --- | --- |
| Date (UTC) | 2026-07-22T10:05:58Z |
| Scenarios | streams |
| kaas image | `ghcr.io/woestebanaan/kaas-preview:0.2.14-preview` |
| kaas replicas (ready/spec) | 3/3 |
| kaas controller | kaas-1 (lease transitions 1033) |
| kaas flushIntervalMessages | 1 |
| strimzi Kafka version | 4.2.0 (3 broker pods) |
| Node | nixos (v1.34.4+k3s1, NixOS 25.11 (Xantusia)) |

### Storage
```
--- kaas ---
  kaas-data                                class=nfs        csi=nfs.csi.k8s.io  192.168.1.51:/mnt/data/k8s-volumes
--- strimzi ---
  data-0-kafka-cluster-dual-role-1         class=nfs        csi=nfs.csi.k8s.io  192.168.1.51:/mnt/data/k8s-volumes
  data-0-kafka-cluster-dual-role-2         class=nfs        csi=nfs.csi.k8s.io  192.168.1.51:/mnt/data/k8s-volumes
  data-0-kafka-cluster-dual-role-3         class=nfs        csi=nfs.csi.k8s.io  192.168.1.51:/mnt/data/k8s-volumes
```

## Results (kaas vs strimzi)


### streams

| cluster | input records | output records | runner s | verdict |
| --- | ---: | ---: | ---: | :---: |
| kaas | 2024 | 43670 | 8 | PASS |
| strimzi | 1009 | 9508 | 8 | PASS |

Completed: 2026-07-22T10:07:34Z

## Correction — this PASS was a false positive

`kaas 43670 / strimzi 9508 output records, runner_wait=8s, PASS` is not a
result. The verdict was `output_records > 0` against `streams-wordcount-output`,
a topic the scenario never resets — so it passed instantly on records left by an
earlier run. On kaas that log file was last written at 05:45, hours before this
run: the topology produced nothing here.

Fixed in the skill: the verdict now baselines the output topic before
unsuspending the runner and requires *new* records.

Streams was validated separately on this deployment — see the correction in
`bench-compare-v2-20260722-090338Z.md` for the controlled feed that drove
input → repartition → changelog → output end to end.
