edition                  = "2021"
max_width                = 100
reorder_imports          = true
use_field_init_shorthand = true

# imports_granularity and group_imports are nightly-only as of rustfmt 1.83.
# Re-enable once they stabilize.
